"use strict";

var striplines = require('./striplines');

var fs = require('fs');

var input = fs.createReadStream('./rsa.csv');
var output = fs.createWriteStream('./output.csv');

input
    .pipe(striplines(10))
    .pipe(output);


